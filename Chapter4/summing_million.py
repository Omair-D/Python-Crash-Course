# Make a list from one to one million
million = list(range(1, 1000001))

# Use min() and max() to see if it actually starts with one and ends in one million
print(min(million))
print(max(million))

# Use sum function to see how fast python can add a million numbers
print(sum(million))